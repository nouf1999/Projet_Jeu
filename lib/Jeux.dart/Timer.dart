/*import 'package:flutter/material.dart';
Duration initialDuration = Duration(minutes: 1); // Durée initiale du chronomètre (1 minute)
Duration elapsedDuration = Duration(); // Temps écoulé
Text('${elapsedDuration.inSeconds} seconds')
@override
void initState() {
  super.initState();
  Timer.periodic(Duration(seconds: 1), (Timer timer) {
    setState(() {
      elapsedDuration += Duration(seconds: 1);
    });
  });
}*/
